{
	"version": "2.0.0",
	"tasks": [
		{
			// For this to work you need to have installed 7z
			"label": "— Build Kaboom",
			"type": "shell",
			"command": "pnpm build",
			"group": {
				"kind": "build",
			}
		},
		{
			// This runs the pnpm build and then runs the butler to send it to itch.io
			"label": "Deliver kaboom to Itch.io",
			"type": "shell",
			"command": "butler push export amyspark-ng/clickery-hexagon-2:html5 --userversion 1.0",
		},
		{
			// This runs the pnpm build and then runs the butler to send it to itch.io
			"label": "Deliver kaboom to Itch.io",
			"type": "shell",
			"command": "pnpm build && butler push export amyspark-ng/clickery-hexagon-2:html5 --userversion 1.0",
		},
	]
}
